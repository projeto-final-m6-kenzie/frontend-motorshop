import styled from 'styled-components'

export const Container = styled.div`
  display: flex;
  flex-direction: column;

  .hora {
    display: flex;
    gap: 7px;
  }

  .card {
    display: flex;
    position: absolute;
    flex-direction: column;

    padding: 1rem;
  }
`
